this application needs java 11 JRE

the data is by default kept in the "workspace" directory

to change the settings open config/application.yaml

    change server port
    change max. file size

to run the application:

    On windows:

        run.bat

    On linux

        chmod u+x run.sh
        ./run.sh

